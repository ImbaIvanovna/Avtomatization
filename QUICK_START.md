# 🚀 Быстрый старт с Docker

## ✅ Ваш проект полностью контейнеризован!

### 🎯 Запуск приложения

```bash
# Production режим (рекомендуется)
docker-compose up -d

# Development режим (с hot reload)
docker-compose -f docker-compose.dev.yml up -d
```

### 🌐 Доступ к приложению

- **Основное приложение**: http://localhost:5000
- **Через nginx** (если включен): http://localhost:80

### 🔧 Управление

```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart
```

### 📊 Проверка работы

Откройте браузер и перейдите по адресу: **http://localhost:5000**

Вы должны увидеть главную страницу музыкального магазина "Мелодия" с:
- ✅ Красивым интерфейсом
- ✅ Статистикой (5 ансамблей, 6 произведений, 6 дисков, 9 музыкантов)
- ✅ Формами входа и регистрации

### 🗄️ Данные

- **База данных**: сохраняется в папке `./data/`
- **Логи**: сохраняются в папке `./logs/`
- **Статические файлы**: монтируются из `./static/`

### 🎵 Готово!

Ваш музыкальный магазин теперь работает в Docker контейнере и готов к использованию!




